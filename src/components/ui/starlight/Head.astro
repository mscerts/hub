---
import StarlightHead from "@astrojs/starlight/components/Head.astro";
import VtbotStarlight from "astro-vtbot/components/starlight/Base.astro";
import CookieConsentBanner from "@components/ui/banners/CookieConsentBanner.astro";
import CookieConsentModal from "@components/ui/modals/CookieConsentModal.astro";
import AnnouncementBanner from "@components/ui/banners/AnnouncementBanner.astro";
import { VERIFICATIONS } from "@data/constants";

const canonicalUrl = new URL(Astro.url.pathname, Astro.site).href;
---

<VtbotStarlight {...Astro.props} viewTransitionsFallback="animate">
  <StarlightHead {...Astro.props}>
    <slot />
  </StarlightHead>
</VtbotStarlight>

<meta property="og:url" content={canonicalUrl} />
<meta name="impact-site-verification" content={VERIFICATIONS.udemy} />
<meta name="mitgo-verification" content={VERIFICATIONS.mitgo} />

<!-- Cookie Consent Components -->
<CookieConsentBanner />
<CookieConsentModal />
<AnnouncementBanner
    btnId="dismiss-button"
    btnTitle="New AI/AB Certifications on the Hub"
    btnTitleMobile="New AI/AB Certs"
    url="/wiki"
    version="3"
/>
<style is:global>
  /* Slow down Chrome's default animation */
  ::view-transition-group(root) {
    animation-duration: 250ms;
  }

  /* Do not slide over the sidebars */
  ::view-transition-group(*) {
    overflow: hidden;
  }
</style>

<script is:inline>
  // Sync theme to new document before View Transition swap
  document.addEventListener("astro:before-swap", (ev) => {
    const newDoc = ev.newDocument;

    // Sync theme: apply both .dark class and data-theme attribute
    const raw = localStorage.getItem("hs_theme") || localStorage.getItem("starlight-theme");
    const theme = (raw === "light" || raw === "dark") ? raw
      : (window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light");
    newDoc.documentElement.classList.toggle("dark", theme === "dark");
    newDoc.documentElement.dataset.theme = theme;

    // Remove dismissed banners from the new document before it swaps in
    newDoc.querySelectorAll("astro-banner").forEach((banner) => {
      const btnId = banner.getAttribute("btnId");
      const version = banner.getAttribute("version") || "1";
      const rememberDismissal = banner.getAttribute("rememberDismissal") !== "false";
      if (rememberDismissal && btnId) {
        const storageKey = `banner-dismissed-${btnId}-v${version}`;
        if (localStorage.getItem(storageKey)) {
          banner.remove();
        }
      }
    });

    // Ensure new stylesheets are loaded before swap
    const oldStyles = [...document.querySelectorAll('link[rel="stylesheet"]')];
    const newStyles = [...newDoc.querySelectorAll('link[rel="stylesheet"]')];
    newStyles.forEach((newStyle) => {
      const href = newStyle.getAttribute('href');
      const existing = oldStyles.find(s => s.getAttribute('href') === href);
      if (!existing) {
        const link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = href;
        document.head.appendChild(link);
      }
    });
  });
</script>

<script is:inline>
  // Initial theme sync on page load (runs after Starlight's ThemeProvider)
  (() => {
    const raw = localStorage.getItem("hs_theme") || localStorage.getItem("starlight-theme");
    const theme = (raw === "light" || raw === "dark") ? raw
      : (window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light");
    document.documentElement.classList.toggle("dark", theme === "dark");
    document.documentElement.dataset.theme = theme;
    localStorage.setItem("hs_theme", theme);
    localStorage.setItem("starlight-theme", theme);
  })();
</script>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const siteSearch = document.querySelector(
      "site-search"
    ) as HTMLElement | null;
    if (siteSearch) {
      siteSearch.setAttribute("data-lenis-prevent", "");
    }
    
    // Prevent Lenis from interfering with sidebar scrolling
    const sidebar = document.querySelector("#starlight__sidebar") as HTMLElement | null;
    if (sidebar) {
      sidebar.setAttribute("data-lenis-prevent", "");
    }
    
    // Also prevent Lenis interference with right sidebar
    const rightSidebar = document.querySelector(".right-sidebar") as HTMLElement | null;
    if (rightSidebar) {
      rightSidebar.setAttribute("data-lenis-prevent", "");
    }
  });
</script>
